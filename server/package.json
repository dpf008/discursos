{
  "name": "server",
  "description": "Deco MCP server built on top of Cloudflare Workers",
  "version": "1.0.0",
  "scripts": {
    "dev": "deco dev --clean-build-dir ./view-build",
    "deploy": "wrangler build && deco deploy ./dist",
    "build": "wrangler build",
    "gen": "deco gen --output=deco.gen.ts",
    "db:generate": "drizzle-kit generate"
  },
  "dependencies": {
    "@deco/workers-runtime": "npm:@jsr/deco__workers-runtime@0.20.1",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "deco-cli": "^0.20.1",
    "drizzle-kit": "^0.31.4",
    "wrangler": "^4.28.0"
  }
}
